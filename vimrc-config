#Some commands that i use in vim:
#//Write the vim commands that delete all the words that contains 'c' in the file.
#:%s/\bc\b//g 

#//Write the vim commands that delete for each line that has the character '>' until end of line.
#:%s/\>//g
#
#Vim commands to use for opening files.
#add github copilot to vim
#:source ~/.vim/bundle/copilot/copilot.vim

#If you want to delete from a certain line until end of file or another line:
# <number_line_to_start (dot for current line)>,<number_line_to_delete>d [or $ for EOF as number_line_to_delete]

#Pasting in line mode 1
#   a<c-r>"
#   Feel free to map it. Eg, nnoremap <leader>p a<c-r>"


#Pasting in line mode 2
#    function! s:PasteInline(afterOrBefore)
#        exe 'let s:myContent = @' . v:register
#        let s:myContent = substitute(s:myContent, '^\s*', '', 'g')
#        let s:myContent = substitute(s:myContent, '\s*\n', '\n', 'g')
#        let s:myContent = substitute(s:myContent, '\n\s*', '\n', 'g')
#        if count(s:myContent, "\n") > 1
#            let s:myContent = substitute(s:myContent, '\n', ' ', 'g')
#            let s:myContent = substitute(s:myContent, ' $', '', 'g')
#        else
#            let s:myContent = substitute(s:myContent, '\n', '', 'g')
#        endif
#        execute 'norm! ' . a:afterOrBefore . s:myContent
#    endfunction
#    nnoremap <expr><silent> gp ':call <SID>PasteInline("a")<CR>'
#    nnoremap <expr><silent> gP ':call <SID>PasteInline("i")<CR>'
#

#TODO: Vim command to comment first n lines above with #

#This is a vimrc file for my vim configuration.

#Create a configuration for vim that has number line, relativenumber, fold, and syntax highlighting. [With copilot]
syntax on
set number
set relativenumber
set foldmethod=marker

set nocompatible
set backspace=indent,eol,start
set autoindent
set smartindent
set shiftwidth=4
set tabstop=4
set expandtab
set smarttab
set softtabstop=4
set shiftround
set textwidth=80
set textwidth=80
set foldlevel=99
set foldmethod=indent
set foldcolumn=0
set foldenable
